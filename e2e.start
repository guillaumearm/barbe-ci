#!/bin/bash

SCRIPTPATH="$( cd "$(dirname "$0")" ; pwd -P )"

docker run -it --rm \
  -v $SCRIPTPATH/e2e:/home/pptruser/e2e/e2e \
  -v $SCRIPTPATH/packages/client/src:/home/pptruser/e2e/packages/client/src \
  -v $SCRIPTPATH/packages/server/src:/home/pptruser/e2e/packages/server/src \
  sdp-e2e
